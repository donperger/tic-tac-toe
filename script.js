const gameBoard = (() => {
    let gridArray = ["X", "O", "X", 
                    "X", "X", "O", 
                    "O", "O", "X"]
})();

const Player = (name) => {
    const name = name;
    let score = 0;
}